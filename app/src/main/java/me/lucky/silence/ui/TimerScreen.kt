package me.lucky.silence.ui

import androidx.compose.runtime.Composable
import me.lucky.silence.Preferences

@Composable
fun TimerScreen(prefs: Preferences, onBack: () -> Unit) {
    // Implementeer hier de UI voor het Timer scherm
}